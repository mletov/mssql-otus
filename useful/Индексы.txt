Индекс - это как оглавление в книге
Неупорядоченный данные - куча (heap)  vs кластеризованная таблица
Индексы быстрые и


В кластерных индексах B+ tree - сбалансированное дерево
Поиск по кластерный индексам


В кластерном идексе на листьях хранятся все данные

Некластерный индекс ссылается на место на диске (Rid lookup)

Tasble scan  И Tasble seek - поиск по B+ tree

Tasble scan - полное сканирование всей таблицы (без учета индекса)
index seek - дял некластерного

WITH INDEX(0) - задать по какому индексу искать

key lookup - подтягивание доп полей

Избавиться от key lookup - ionclude (fieldName) - последующйи поиск по сдел колонкам

rid lookup тоже самое что и key lookup 
план запроса;
index scan;
index seek;
nested Loop;
hash match;
merge join.

Если запросе задействованы все поля - покрывающий

- Обычные

- Полнотекстовые
Можно искать не только в полях (падежи), но ив текстовых файлах
Стоп слова (is, the и пр)
Можно задать еще список синонимов

- Колоночные
- Пространственные
- XML